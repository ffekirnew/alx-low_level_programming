#include "main.h"

/**
 * print_numbers - Entry point to the program
 * Return: Always 0 (Success)
 */
void print_numbers(void)
{
	int a;

	for (a = 48; a < 58; a++)
	{
		_putchar(a);
	}
	_putchar(10);
}
